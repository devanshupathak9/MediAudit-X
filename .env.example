# Copy to .env (project root, gitignored) and fill in:  cp .env.example .env
# NEVER put keys in elasticsearch/.env -- that file is committed to git.

# openai | local
EMBEDDING_PROVIDER=openai
OPENAI_API_KEY=sk-...
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

ES_URL=http://localhost:9200
